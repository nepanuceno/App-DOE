import { SafeAreaView, Text } from "react-native";

const SettingsScreen = () => {
    return (
        <SafeAreaView>
        <Text>Busca por Edição!</Text>
        </SafeAreaView>
    );
};

export default SettingsScreen;